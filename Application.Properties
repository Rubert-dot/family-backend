# ---------- Server ----------
server.port=8080
server.address=0.0.0.0

# ---------- MySQL ----------
# Create the database first:  CREATE DATABASE family_memories;
spring.datasource.url=jdbc:mysql://mysql-2fc38eac-family-project.h.aivencloud.com:18122/defaultdb?sslmode=require
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_le82pI3WIVasbrAmia5
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---------- JPA / Hibernate ----------
spring.jpa.hibernate.ddl-auto=update    
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ---------- File upload (photos) ----------
# Uploaded photos are saved to this folder on disk and served back at /uploads/**
app.upload.dir=uploads
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=60MB

# ---------- CORS ----------
# React dev server default port
app.cors.allowed-origin=http://localhost:5173

# ---------- Family login ----------
# ONE login for the whole site. You set these, then share with family if needed.
# CHANGE THESE before you deploy anywhere public.
app.security.family-email=admin@ourfamily.com
app.security.family-password=secret123


server.tomcat.basedir=target/tomcat